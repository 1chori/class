<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    반갑습니다. <%= data.nickName %>님!
    <table>
        <tr>
            <th>번호</th>
            <th>타이틀</th>
            <th>작성자</th>
            <th>삭제</th>
        </tr>
        <% post.forEach((el, index) => { %>
            <tr>
                <td><%= el.id %></td>
                <td><a href="/board/view/<%= el.id %>"><%= el.title %></a></td>
                <td><%= el.writer %></td>
                <td><a href="/board/delete_board/<%= el.id %>">삭제</a></td>
            </tr>
        <% }); %>
    </table>
    <a href="/board/create_board">글 쓰기</a>
</body>
</html>